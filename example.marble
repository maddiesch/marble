// Define Greetings
const greeting_names = ["Maddie Schipper", "from Marble"]

const greeting = fn(message) {
  print_description("Hello " + message + "!")
}

let index = 0
while (index < len(greeting_names)) {
  greeting(greeting_names[index])

  index = index + 1
}
